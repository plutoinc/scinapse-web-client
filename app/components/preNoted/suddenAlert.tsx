import * as React from "react";
import { withStyles } from "../../helpers/withStylesHelper";
const styles = require("./suddenAlert.scss");

const SuddenAlert: React.FC<{}> = () => {
  return <div />;
};

export default withStyles<typeof styles>(styles)(SuddenAlert);
